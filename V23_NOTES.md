# V23 — Asset Integration + Hover Discipline

- Home treasure cards remain visually clean at rest; dramatic environments now reveal on pointer hover only.
- Creature Index uses the approved more-realistic pixel manta artwork and gliding hover treatment.
- Encyclopedia removes the stray flying-page treatment and visually reconnects the open book around a central spine/base.
- Restored heart marker is moved inside the card safe area so it cannot be clipped.
- Ashe/Otter selector no longer adds an extra right-side heart; selector and cursor companion now use the supplied character artwork.
- Decide For Me uses the original generated black-cat Master of the Project Lab artwork instead of the CSS block wizard, while preserving the every-click jump/cast + speech behavior.
- Decide For Me title/subtitle receive protected layout space to prevent overlap.
- Graveyard filter displays the supplied moonlit Project Graveyard artwork as a static banner above the grave plots.
- Boot remains the initial React screen on every full app mount/reload; no local/session persistence is used to skip it.
